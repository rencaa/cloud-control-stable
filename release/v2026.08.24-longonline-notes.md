长期在线版在 v2026.08.24-final 基础上继续增强，原最终版保留不变。

主要更新：

- 局域网 MQTT 3.1.1 主通道、QoS 1、PUBACK 重试和 PINGRESP 看门狗，WebSocket 自动回退。
- 服务端 durable outbox 默认开启：指数退避、最长保留 7 天、设备重连立即补发。
- EasyClick v92 本地持久化任务状态和 ACK，收到服务端 event_ack 后才删除。
- 每次任务增加 run_id，旧轮次迟到状态不会污染新轮次；离线设备只保留最新轮次。
- cron 下一次运行时间持久化，服务停机 24 小时内最多补跑一次，避免恢复任务风暴。
- 管理后台任务列表在页面可见时每 3 秒自动更新。
- 局域网免令牌模式只把 MQTT 绑定到选中的私网 IP，避免暴露到公网接口。
- 移除手机源码中的历史 webhook，改为可选本机配置。

一键安装（Ubuntu 24 / 局域网）：

```bash
cd /tmp && curl -fL --retry 5 -O https://work.kd99.cn/https://github.com/rencaa/cloud-control-stable/releases/latest/download/cloud-control-stable-cn-fast.tar.gz -O https://work.kd99.cn/https://github.com/rencaa/cloud-control-stable/releases/latest/download/cloud-control-stable-cn-fast.tar.gz.sha256 && sha256sum -c cloud-control-stable-cn-fast.tar.gz.sha256 && tar -xzf cloud-control-stable-cn-fast.tar.gz && sudo bash cloud-control-cn/install-cn.sh --lan
```

SHA-256：

- cloud-control-stable-cn-fast.tar.gz: `9ffcaa7b2dd68cbda98c938a576b091ea4a6092f09aabf56a7f84481976ef3e7`
- cloud-control-easyclick-lan-v92.zip: `df1d6079b65ebec1ffbfd98b6c2123ec8dfbda651a9f13af280ca2482cd49251`

回滚：`sudo cloud-control-native-rollback`
